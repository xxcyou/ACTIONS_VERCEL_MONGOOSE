{
      "version": 2,
      "routes": [
            {
                  "src": "/(.*)",
                  "dest": "index.js"
            }
      ],
      "env": {
            "MONGO_URI": "${{ secrets.MONGO_URI }}"
      }
}